import 'package:flutter/material.dart';

class RemiseScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("REMISE"),
      ),
    );
  }
}
